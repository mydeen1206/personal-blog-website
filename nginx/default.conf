server {
    listen 80;
    server_name www.kubepilot.net;

    return 301 http://personalblog.kubepilot.net$request_uri;
}

server {
    listen 80;
    server_name personalblog.kubepilot.net;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
